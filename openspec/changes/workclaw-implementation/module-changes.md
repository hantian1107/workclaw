# Workclaw 模块文件改动预览

## 概述

本文档用于预览Workclaw项目各个模块的文件改动和目的，详细说明每个文件的功能和实现目标，帮助开发团队理解项目结构和开发计划。

## 1. 核心模块 (src/agent/)

### 1.1 智能体循环 (src/agent/)

| 文件         | 改动内容           | 目的                                                |
| ------------ | ------------------ | --------------------------------------------------- |
| `agent.ts` | 创建智能体循环实现 | 实现智能体循环，处理用户输入，调用LLM，处理工具调用 |

### 1.2 运行时环境 (src/agent/runtime/)

| 文件           | 改动内容           | 目的                                                |
| -------------- | ------------------ | --------------------------------------------------- |
| `session.ts` | 创建会话管理模块   | 管理Channel到Workspace的映射，维护用户会话状态      |
| `context.ts` | 创建上下文记忆模块 | 维护对话历史和上下文信息，提供记忆能力              |

### 1.3 工具函数 (src/agent/utils/)

| 文件                | 改动内容           | 目的                                   |
| ------------------- | ------------------ | -------------------------------------- |
| `context.ts`      | 创建上下文管理工具 | 管理智能体上下文信息                   |
| `conversation.ts` | 创建对话管理工具   | 管理对话历史和状态                     |
| `prompts.ts`      | 创建提示词管理工具 | 管理系统提示词、角色提示词和特殊提示词 |

### 1.4 LLM服务 (src/agent/llm/)

| 文件              | 改动内容    | 目的                          |
| ----------------- | ----------- | ----------------------------- |
| `llmService.ts` | 创建LLM服务 | 封装LLM API调用，提供模型服务 |

### 1.5 技能层 (src/agent/skills/)

//这里不进行改动，因为skill是最后才加入

| 文件        | 改动内容             | 目的                                       |
| ----------- | -------------------- | ------------------------------------------ |
| `coding/` | 创建代码助手技能目录 | 实现代码审查、生成、重构等代码相关功能     |
| `writer/` | 创建写作技能目录     | 实现文档生成、内容编辑、格式转换等写作功能 |
| `search/` | 创建搜索技能目录     | 实现文件搜索、内容搜索、信息提取等搜索功能 |

### 1.6 类型定义 (src/agent/)

| 文件         | 改动内容               | 目的                                        |
| ------------ | ---------------------- | ------------------------------------------- |
| `types.ts` | 创建智能体相关类型定义 | 定义Agent相关的TypeScript类型，确保类型安全 |

## 2. 系统内核 (src/core/)

### 2.1 安全沙箱 (src/core/)

| 文件           | 改动内容             | 目的                                     |
| -------------- | -------------------- | ---------------------------------------- |
| `sandbox.ts` | 创建逻辑沙箱模块     | 拦截能力调用，检查操作权限，确保安全执行 |
| `core.ts`    | 创建核心主进程       | 解析指令，管理能力调用                   |
| `config.ts`  | 创建全局配置管理模块 | 管理应用配置，提供配置读写能力           |

## 3. 工作空间模块 (src/workspace/)

| 文件             | 改动内容             | 目的                             |
| ---------------- | -------------------- | -------------------------------- |
| `workspace.ts` | 创建工作空间管理模块 | 管理工作空间配置，维护资源白名单 |

## 4. 能力层 (src/core/capabilities/)

| 文件           | 改动内容           | 目的                                   |
| -------------- | ------------------ | -------------------------------------- |
| `file.ts`    | 创建文件操作能力   | 提供文件读写、删除、列出等文件系统操作 |
| `shell.ts`   | 创建系统命令能力   | 提供系统命令执行、进程管理等功能       |
| `browser.ts` | 创建浏览器控制能力 | 提供网页打开、内容获取等浏览器操作     |

## 5. 接入层 (src/channels/)

### 5.1 接口定义 (src/channels/)

| 文件        | 改动内容         | 目的                                   |
| ----------- | ---------------- | -------------------------------------- |
| `base.ts` | 创建IChannel接口 | 定义通道适配器的统一接口，规范通道行为 |

### 5.2 通道实现 (src/channels/)

| 文件        | 改动内容             | 目的                                                               |
| ----------- | -------------------- | ------------------------------------------------------------------ |
| `ipc/`    | 创建Electron IPC通道 | 处理来自渲染进程的消息，实现本地GUI交互                            |
| `feishu/` | 创建飞书Webhook通道  | 处理来自飞书的远程消息，实现远程IM交互（注：仅预留空间，后续开发） |

## 6. 主进程 (src/main/)

| 文件           | 改动内容           | 目的                                  |
| -------------- | ------------------ | ------------------------------------- |
| `index.ts`   | 创建应用初始化模块 | 初始化应用，组装各个模块，启动服务    |
| `preload.ts` | 创建预加载脚本     | 安全桥接主进程和渲染进程，提供API访问 |
| `window.ts`  | 创建窗口管理模块   | 管理应用窗口的创建、显示、隐藏等操作  |

## 7. 渲染进程 (src/renderer/)

### 7.1 UI组件 (src/renderer/components/)

| 文件            | 改动内容       | 目的                           |
| --------------- | -------------- | ------------------------------ |
| `components/` | 创建UI组件目录 | 存放React UI组件，构建用户界面 |

### 7.2 工具和状态 (src/renderer/)

| 文件        | 改动内容            | 目的                                  |
| ----------- | ------------------- | ------------------------------------- |
| `hooks/`  | 创建React Hooks目录 | 存放自定义Hooks，封装业务逻辑         |
| `stores/` | 创建状态管理目录    | 存放Zustand状态管理存储，管理应用状态 |
| `views/`  | 创建页面视图目录    | 存放应用页面组件，构建完整界面        |

### 7.3 应用入口 (src/renderer/)

| 文件         | 改动内容         | 目的                            |
| ------------ | ---------------- | ------------------------------- |
| `App.tsx`  | 创建应用入口组件 | 应用的主组件，组织页面结构      |
| `main.tsx` | 创建渲染入口文件 | 渲染进程的入口点，启动React应用 |

## 8. 改动优先级

1. **核心模块**：实现core.ts、sandbox.ts和基本能力
2. **工作空间模块**：实现workspace.ts和工作空间管理功能
3. **智能体模块**：实现agent.ts和智能体循环
4. **LLM模块**：实现llmService.ts和模型调用
5. **接入层**：实现IPC通道，确保本地GUI交互
6. **主进程**：实现应用初始化和窗口管理
7. **渲染进程**：实现用户界面
8. **技能模块**：实现各种业务技能
9. **飞书通道**：最后实现，作为扩展功能

## 9. 技术实现要点

- **模块化设计**：每个模块职责清晰，接口明确
- **类型安全**：使用TypeScript确保类型安全
- **安全优先**：实现严格的沙箱机制，确保系统安全
- **可扩展性**：设计灵活的插件系统，支持能力和技能的扩展
- **性能优化**：实现流式处理，优化大文件操作性能
- **标准化工具调用**：实现工具调用指令JSON结构设计，标准化工具调用和响应
- **智能体循环**：实现完整的智能体循环，处理用户输入和工具调用
- **LLM服务**：封装LLM API调用，提供模型服务

## 10. 测试策略

- **单元测试**：测试各个模块的核心功能
- **集成测试**：测试模块间的交互
- **端到端测试**：测试完整的用户流程
- **安全测试**：测试沙箱机制的有效性
- **性能测试**：测试大文件处理和系统响应速度

## 11. 部署计划

- **开发环境**：使用Vite进行开发和热重载
- **构建打包**：使用Electron Builder打包应用
- **发布渠道**：提供Windows、macOS和Linux版本
- **版本管理**：使用语义化版本控制，定期发布更新
