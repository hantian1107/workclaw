现在我需要开发一个智能体。

如果用一个词来形容，就是限制版的openclaw

该智能体有以下核心功能

1，文件打开与编辑

2，工作空间添加：工作空间就是一系列文件夹，可以视为一个工作区域。

当然了，其保存的是某个文件夹的链接，因此可以打开位于很多地方的文件。如果更深层次，允许在工作空间中添加应用

3，智能体对话

4，通过对话，智能体可以在工作空间中进行工作

5，允许连接飞书远程调用智能体

由于是本地使用，所以需要打包成客户端。

## 技术栈选择

### 前端
- **React + TypeScript**：现代化的前端框架，适合构建复杂的用户界面，提供良好的类型安全性
- **Electron**：用于将 Web 应用打包成跨平台桌面客户端
- **Tailwind CSS**：用于快速构建美观的响应式界面
- **组件库**：**shadcn/ui**，与 Tailwind CSS 完美集成，提供现代化、美观的 UI 组件，适合快速构建响应式界面
- **状态管理**：Redux 或 Zustand 用于管理应用状态

### 后端/智能体逻辑
- **Node.js**：用于构建后端服务，处理文件操作和智能体逻辑
- **LLM API**：集成 OpenAI API 或其他大语言模型 API 用于智能体对话功能
- **文件系统操作**：使用 Node.js 的 fs 模块或更高级的文件操作库

### 飞书集成
- **飞书开放平台 SDK**：用于与飞书集成，实现远程调用智能体功能

### 构建与部署
- **Vite**：现代化的构建工具，提供快速的开发体验
- **Electron Builder**：用于将应用打包成桌面安装包

## 项目结构

```
workclaw/
├── public/                  # 静态资源
├── src/
│   ├── main/                # Electron 主进程
│   │   ├── index.ts         # 主进程入口
│   │   ├── fileSystem.ts    # 文件系统操作
│   │   └── feishu.ts        # 飞书集成
│   ├── renderer/            # Electron 渲染进程
│   │   ├── components/      # 组件
│   │   │   ├── Workspace/   # 工作空间相关组件
│   │   │   ├── Editor/      # 编辑器相关组件
│   │   │   └── Feishu/      # 飞书集成组件
│   │   ├── pages/           # 页面
│   │   │   ├── Home/        # 首页
│   │   │   ├── Workspace/   # 工作空间页面
│   │   │   └── Settings/    # 设置页面
│   │   ├── hooks/           # 自定义 hooks
│   │   ├── store/           # 状态管理
│   │   ├── utils/           # 工具函数
│   │   ├── App.tsx          # 应用入口
│   │   └── index.tsx        # 渲染进程入口
│   ├── agent/               # 智能体相关代码
│   │   ├── core/            # 智能体核心逻辑
│   │   │   ├── llm.ts       # LLM API 集成
│   │   │   ├── context.ts   # 对话上下文管理
│   │   │   └── executor.ts  # 命令执行器
│   │   ├── components/      # 智能体相关组件
│   │   │   └── Chat/        # 智能体对话组件
│   │   └── services/        # 智能体服务
│   │       └── handler.ts   # 智能体请求处理器
│   └── common/              # 公共代码
│       ├── types/           # TypeScript 类型定义
│       └── constants/       # 常量
├── package.json             # 项目配置
├── tsconfig.json            # TypeScript 配置
├── vite.config.ts           # Vite 配置
└── electron-builder.json    # Electron Builder 配置
```

## 核心模块设计

### 1. 文件系统模块
- 负责文件的打开、编辑、保存等操作
- 支持工作空间中的文件管理
- 提供文件系统监控功能

### 2. 工作空间模块
- 管理工作空间的创建、编辑、删除
- 支持添加文件夹到工作空间
- 支持工作空间的导入导出

### 3. 智能体模块
- **核心逻辑**：与 LLM API 集成，处理用户对话，管理对话上下文
- **命令执行**：根据对话内容执行文件操作等命令
- **服务接口**：提供智能体服务的统一接口
- **对话组件**：提供智能体对话界面

### 4. 飞书集成模块
- 处理飞书的授权和认证
- 接收飞书的远程调用请求
- 将智能体的响应返回给飞书

### 5. 客户端打包模块
- 负责应用的打包和分发
- 支持 Windows、macOS、Linux 平台
- 处理应用的自动更新
